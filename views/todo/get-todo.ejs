<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8" />
  <meta name="viewport" content="width=device-width, initial-scale=1.0"/>
  <title>Todo List</title>
  <script src="https://cdn.tailwindcss.com"></script>
</head>
<body class="bg-gradient-to-r from-blue-50 to-purple-100 min-h-screen p-6 flex flex-col items-center">

  <div class="w-full max-w-3xl">
    <h1 class="text-4xl font-bold text-center text-gray-800 mb-10">📝 My Todo List</h1>

    <% if (todos && todos.length > 0) { %>
      <div class="grid gap-6 sm:grid-cols-2">
        <% todos.forEach(function(todo) { %>
          <div class="bg-white rounded-xl shadow-md hover:shadow-lg transition p-6 border-l-4 
                      <%= todo.completed ? 'border-green-500' : 'border-yellow-400' %>">
            <div class="flex items-center justify-between mb-2">
              <h2 class="text-xl font-semibold text-gray-900"><%= todo.task %></h2>
              <% if (todo.completed) { %>
                <span class="inline-flex items-center text-green-600 font-medium text-sm">
                  ✅ Done
                </span>
              <% } else { %>
                <span class="inline-flex items-center text-yellow-600 font-medium text-sm">
                  ⏳ Pending
                </span>
              <% } %>
            </div>
            <p class="text-gray-600 text-sm mb-2"><%= todo.description || 'No description provided.' %></p>
            <p class="text-xs text-gray-500">📅 <%= todo.date || 'No date' %></p>
          </div>
        <% }); %>
      </div>
    <% } else { %>
      <div class="text-center text-gray-700 mt-20">
        <p class="text-lg font-medium">No todos yet! Add one to get started 🚀</p>
      </div>
    <% } %>
  </div>

</body>
</html>
